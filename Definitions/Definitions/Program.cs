using System;

namespace Definitions
{
    class Program
    {
        static void Main(string[] args)
        {
            //Statement - defines a variable//
            var month = 10;

            //Expresion - creates a term//
            if (month > 10)

            //Operator - the action piece ("+") //
            month = month + 2;

            //Operand - the pieces being acted on ("month" and "2")//
            month = month + 2;

        }
    }
}
